---
description: 显示全局优化统计
allowed-tools: ["Read", "Glob"]
---

# 全局优化统计

你需要汇总所有需求优化项目的数据，生成全局统计报告。

## 执行步骤

### 1. 扫描所有项目

使用 Glob 扫描以下路径，收集所有项目数据：
- `.require-agent/projects/*/state.json` — 项目状态
- `.require-agent/projects/*/scores/*.json` — 所有评分记录

如果没有找到任何项目，提示：
```
未找到任何需求优化项目，无统计数据。
使用 /require "需求描述" 启动第一个项目。
```

### 2. 读取所有项目数据

逐个读取每个项目的 `state.json` 和 `scores/` 下的所有评分文件。

### 3. 计算统计指标

#### a) 基础统计

- **总项目数**：项目目录总数
- **已完成项目数**：`phase === "completed"` 的项目数
- **进行中项目数**：其余项目数
- **完成率**：已完成 / 总项目数

#### b) 轮次统计

- **平均轮次**：所有已完成项目的 `current_round` 平均值
- **最少轮次**：已完成项目中最少的轮次数及项目名
- **最多轮次**：已完成项目中最多的轮次数及项目名

#### c) 评分统计

- **平均初始分**：所有项目 `round-0.json` 的 `average` 平均值
- **平均最终分**：所有已完成项目最终评分的 `average` 平均值
- **平均提升幅度**：最终分 - 初始分 的平均值
- **最高最终分**：所有项目中最高的最终平均分及项目名

#### d) 维度统计

对每个维度（completeness、consistency、feasibility、user_journey、business_loop 以及按需维度）：
- 计算所有项目中该维度的平均初始分和平均最终分
- 找出提升幅度最大和最小的维度

#### e) 策略效果统计（最有效策略 TOP5）

遍历所有项目的 `round_history`，统计每种 `target_dimension` + `action` 组合的出现次数和成功率（`action === "improved"` 视为成功）：

按成功率排序，取 TOP5。

#### f) 停滞统计

- **最常停滞维度**：在所有项目中出现在 `stalled_dimensions` 中次数最多的维度
- **从未停滞维度**：从未出现在任何项目的 `stalled_dimensions` 中的维度

### 4. 展示统计报告

```
全局优化统计

---

## 概览

| 指标 | 数值 |
|------|------|
| 总项目数 | {总数} |
| 已完成 | {完成数}（{完成率}%） |
| 进行中 | {进行中数} |

## 轮次统计

| 指标 | 数值 |
|------|------|
| 平均轮次 | {平均轮次} |
| 最少轮次 | {最少轮次}（{项目名}） |
| 最多轮次 | {最多轮次}（{项目名}） |

## 评分统计

| 指标 | 数值 |
|------|------|
| 平均初始分 | {分数} |
| 平均最终分 | {分数} |
| 平均提升幅度 | +{分数} |
| 最高最终分 | {分数}（{项目名}） |

## 维度表现

| 维度 | 平均初始分 | 平均最终分 | 平均提升 |
|------|-----------|-----------|---------|
| 完整性 | {分数} | {分数} | +{分数} |
| 一致性 | {分数} | {分数} | +{分数} |
| 可行性 | {分数} | {分数} | +{分数} |
| 用户旅程 | {分数} | {分数} | +{分数} |
| 业务闭环 | {分数} | {分数} | +{分数} |
{如果有按需维度的数据，也列出}

提升最大：{维度名}（+{分数}）
提升最小：{维度名}（+{分数}）

## 最有效策略 TOP5

| 排名 | 目标维度 | 成功次数 | 总次数 | 成功率 |
|------|---------|---------|--------|--------|
| 1 | {维度} | {成功数} | {总数} | {百分比}% |
| 2 | {维度} | {成功数} | {总数} | {百分比}% |
| 3 | {维度} | {成功数} | {总数} | {百分比}% |
| 4 | {维度} | {成功数} | {总数} | {百分比}% |
| 5 | {维度} | {成功数} | {总数} | {百分比}% |

## 停滞分析

最常停滞维度：{维度名}（{次数} 次）
从未停滞维度：{维度名列表}

---

数据来源：.require-agent/projects/ 下 {项目数} 个项目
```
